#include <bits/stdc++.h>
using namespace std;
int N;
long long a;
int main () {
    cin.sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> N;
    for (int i=0; i<N; i++) {
        cin >> a;
        cout << a << "\n";
    }
    return 0;
}